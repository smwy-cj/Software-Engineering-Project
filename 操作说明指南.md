# 操作说明指南 - CampusHub（青隅）校园互助服务平台

> 版本：v1.1.0  
> 更新日期：2026-06-04  
> 适用范围：当前仓库根目录 `Software-Engineering-Project/`

---

## 目录

1. [系统概述](#一系统概述)
2. [环境准备](#二环境准备)
3. [项目结构](#三项目结构)
4. [快速启动](#四快速启动)
5. [登录与账号](#五登录与账号)
6. [用户操作指南](#六用户操作指南)
7. [管理员操作指南](#七管理员操作指南)
8. [API 接口说明](#八api-接口说明)
9. [数据库说明](#九数据库说明)
10. [测试与构建](#十测试与构建)
11. [常见问题](#十一常见问题)
12. [开发维护指南](#十二开发维护指南)

---

## 一、系统概述

**CampusHub（青隅）** 是面向高校学生的校园互助与轻社交平台，当前提供以下核心能力：

| 模块 | 入口路由 | 主要功能 |
|------|----------|----------|
| 首页 | `/#/` | 展示最新树洞、快速入口、热门话题、未读通知概览 |
| 树洞 | `/#/treehole` | 匿名动态发布、分类筛选、关键词搜索、点赞、评论 |
| 找搭子 | `/#/partner` | 浏览搭子需求、发布需求、申请加入、撤销需求、处理申请、评价 |
| 恋爱交友 | `/#/love` | 发布交友需求、浏览交友卡片、发送心动、查看双向匹配 |
| 通知中心 | `/#/notifications` | 查看通知、全部已读、处理搭子申请通知 |
| 个人中心 | `/#/profile` | 实名认证、完善资料、查看我的发布/申请、提交反馈 |
| 管理后台 | `/#/admin` | 内容审核、用户处罚、反馈处理 |

### 技术架构

```text
前端：Vue 3 + Vite + Pinia + Vue Router
  开发地址：http://localhost:3000
  API 基础路径：/api/v1
  开发代理：/api -> http://localhost:8080

后端：Spring Boot 3.2.5 + Spring Security + Spring Data JPA
  服务地址：http://localhost:8080
  认证方式：JWT Bearer Token
  开发数据库：H2 内存数据库
```

---

## 二、环境准备

### 2.1 必需软件

| 软件 | 建议版本 | 验证命令 |
|------|----------|----------|
| JDK | 17 或更高 | `java -version` |
| Maven | 3.9 或更高 | `mvn -version` |
| Node.js | 18 或更高 | `node -version` |
| npm | 9 或更高 | `npm -version` |

项目后端目录包含 `mvnw`，在没有全局 Maven 的环境中可优先使用 Maven Wrapper。Windows 环境如果没有 `mvn` 命令，可在 `backend` 目录执行 `.\mvnw.cmd spring-boot:run`。

### 2.2 获取项目

```bash
git clone <仓库地址>
cd Software-Engineering-Project
```

---

## 三、项目结构

```text
Software-Engineering-Project/
├── README.md
├── 操作说明指南.md
├── backend/
│   ├── pom.xml
│   ├── mvnw / mvnw.cmd
│   ├── src/main/java/com/campushub/
│   │   ├── CampusHubApplication.java
│   │   ├── common/          # 统一响应、分页结果、业务异常、全局异常处理
│   │   ├── config/          # 安全配置、启动数据初始化
│   │   ├── controller/      # Auth、TreeHole、Partner、Love、Notification、Profile、Feedback、Admin
│   │   ├── dto/request/     # 请求与响应 DTO
│   │   ├── entity/          # JPA 实体
│   │   ├── repository/      # Spring Data Repository
│   │   ├── security/        # JWT 生成校验与认证过滤器
│   │   └── service/         # 业务服务
│   ├── src/main/resources/
│   │   └── application.yml
│   └── src/test/java/com/campushub/
│       ├── service/
│       └── integration/
├── frontend/
│   ├── package.json
│   ├── vite.config.js
│   ├── index.html
│   └── src/
│       ├── api/index.js           # Axios 实例、JWT 注入、统一错误处理
│       ├── assets/                # 首页、树洞、搭子、通知、恋爱页面图片资源
│       ├── components/ToastHost.vue
│       ├── composables/           # useAsyncState、usePagination、useToast
│       ├── router/index.js        # 前端路由与权限守卫
│       ├── store/auth.js          # 登录态、用户信息、未读通知
│       ├── styles/                # 分层样式文件
│       └── views/                 # 页面组件
└── docs/
    ├── P0/ P1/ P2/ P3/ P4/
    └── backend/
```

请忽略 `frontend/dist/`、`frontend/node_modules/`、`backend/target/` 这类构建或依赖产物，它们不是主要源码目录。

---

## 四、快速启动

建议分别打开两个终端，一个运行后端，一个运行前端。

### 4.1 启动后端

```bash
cd backend
mvn spring-boot:run
```

如果本机没有全局 Maven：

```bash
cd backend
./mvnw spring-boot:run
```

Windows PowerShell 可使用：

```powershell
cd backend
.\mvnw.cmd spring-boot:run
```

后端启动成功后访问地址为 `http://localhost:8080`。控制台出现类似 `Started CampusHubApplication` 表示启动完成。

### 4.2 启动前端

```bash
cd frontend
npm install
npm run dev
```

前端默认运行在 `http://localhost:3000`。浏览器访问：

```text
http://localhost:3000
```

前端使用 Hash 路由，页面地址通常形如 `http://localhost:3000/#/treehole`。

### 4.3 自定义后端代理地址

前端开发代理默认转发到 `http://localhost:8080`。如后端运行在其他地址，可设置环境变量：

```bash
VITE_API_PROXY_TARGET=http://localhost:8081 npm run dev
```

Windows PowerShell：

```powershell
$env:VITE_API_PROXY_TARGET="http://localhost:8081"
npm run dev
```

---

## 五、登录与账号

### 5.1 演示账号

后端启动时会自动初始化演示用户：

| 角色 | 用户名 | 手机号 | 密码 |
|------|--------|--------|------|
| 普通用户 | 小明同学 | `13800138001` | `Abc12345` |
| 普通用户 | 小红同学 | `13800138002` | `Abc12345` |
| 普通用户 | 小刚同学 | `13800138003` | `Abc12345` |
| 管理员 | 管理员 | `13800138000` | `Admin123` |

### 5.2 登录

1. 访问 `http://localhost:3000/#/login`。
2. 输入手机号和密码。
3. 登录成功后，前端会保存 JWT 到浏览器 `localStorage`，后续接口自动携带 `Authorization: Bearer <token>`。

### 5.3 注册

1. 访问 `http://localhost:3000/#/register`。
2. 填写手机号、用户名、密码。
3. 发送短信验证码，开发演示环境可按页面提示使用固定验证码。
4. 注册成功后返回登录页。

---

## 六、用户操作指南

### 6.1 首页

访问 `/#/` 后可看到：

- 快速入口：树洞、找搭子、恋爱交友、通知中心。
- 最新树洞动态：后端可用时读取真实动态；后端未启动或暂无数据时显示本地兜底内容。
- 未读通知概览：登录后展示当前未读数量。

### 6.2 个人中心与实名认证

入口：`/#/profile`，需要登录。

实名认证步骤：

1. 进入个人中心。
2. 在“实名认证”区域填写学号、真实姓名、身份证号、学校、专业、年级、性别、年龄。
3. 点击“提交认证”。
4. 认证成功后页面切换为“完善个人资料”区域。

完善个人资料步骤：

1. 修改用户名、性别、年龄、入学年份、专业、身高、个性签名。
2. 可选择头像图片，图片大小不能超过 512KB。
3. 点击“保存个人资料”。

个人中心还可查看：

- “我的发布”：树洞发布、搭子发布、交友需求。
- “我的申请”：搭子申请、心动申请。
- “提交反馈”：选择问题反馈、功能建议、内容举报或其他，并填写内容。

### 6.3 树洞

入口：`/#/treehole`。

浏览动态：

1. 使用分类下拉框筛选情感、学习、生活、趣味、其他。
2. 在搜索框输入关键词，按 Enter 搜索。
3. 使用排序下拉框切换“最新发布”或“最热”。
4. 点击动态卡片进入详情页。

发布动态：

1. 登录后点击“写下心事”。
2. 选择分类。
3. 输入不超过 500 字的内容。
4. 按需勾选“匿名发布”。
5. 点击“发布”。

点赞与评论：

1. 进入 `/#/treehole/{postId}`。
2. 点击“点赞”或“取消点赞”。
3. 登录后可在评论框输入不超过 100 字内容并发表评论。

### 6.4 找搭子

入口：`/#/partner`。

浏览搭子：

1. 按“全部、学习、运动、吃饭、出行、摄影、其他”等标签筛选。
2. 在搜索框输入关键词并按 Enter 搜索。
3. 查看卡片中的类型、剩余天数、人数、状态、发布者信息。

发布搭子需求：

1. 登录后点击“发起搭子邀请”或访问 `/#/partner/create`。
2. 选择搭子类型。
3. 填写有效天数，范围 1-7 天。
4. 填写最大人数，范围 1-10 人。
5. 输入需求描述。
6. 点击“发布”，成功后返回搭子列表。

申请加入：

1. 在搭子列表中点击“申请加入”。
2. 填写申请附言，最多 120 字，可留空。
3. 点击“发送申请”。
4. 发布者会在通知中心收到申请通知。

撤销需求：

1. 自己发布的搭子卡片会显示“撤销”按钮。
2. 点击后确认撤销，撤销后该需求不再继续招募。

处理搭子申请：

1. 发布者进入 `/#/notifications`。
2. 打开搭子申请相关通知。
3. 查看申请人、搭子需求、申请附言。
4. 点击“接受”或“拒绝”。

查看匹配与评价：

1. 访问匹配详情页 `/#/partner/match/{matchId}`。
2. 匹配状态为 `ACCEPTED` 时可提交 1-5 星评分和 10-100 字评价。

### 6.5 恋爱交友

入口：`/#/love`。

浏览交友需求：

1. 使用“最新发布、最新互动、热门推荐”切换排序视图。
2. 使用分类标签筛选学习交流、兴趣爱好、一起运动、电影音乐、旅行探索、其他。
3. 查看交友卡片中的发布者信息、交友描述、兴趣标签、有效天数。

发布交友需求：

1. 点击“发布交友需求”。
2. 输入不超过 200 字的交友描述。
3. 设置有效天数，范围 1-14 天。
4. 点击“发布”。
5. 发布后进入内容审核，审核通过后展示。

发送心动：

1. 在非本人发布的交友卡片上点击“打个招呼”。
2. 系统记录单向心动。
3. 双方互相心动后，可在“我的匹配”查看。

查看恋爱匹配：

1. 点击恋爱页右上角“我的匹配”。
2. 或直接访问 `/#/love/matches`。

### 6.6 通知中心

入口：`/#/notifications`，需要登录。

可执行操作：

- 按“全部、互动、审核、系统、已读”筛选通知。
- 点击未读通知会自动标记为已读。
- 点击“全部已读”可一次性清空未读状态。
- 搭子申请通知可展开详情，并由发布者接受或拒绝。

### 6.7 反馈

入口：`/#/profile`。

1. 在“提交反馈”区域选择类型。
2. 输入反馈内容，最长 500 字。
3. 点击“提交反馈”。
4. 管理员可在后台查看并处理。

---

## 七、管理员操作指南

入口：`/#/admin`，必须使用管理员账号登录。

### 7.1 内容审核

1. 登录管理员账号 `13800138000 / Admin123`。
2. 进入“管理后台”。
3. 在“待审核内容”列表查看内容类型、内容 ID、用户 ID、提交时间和内容快照。
4. 点击“通过”或“驳回”。

### 7.2 用户管理

1. 输入目标用户 ID。
2. 选择处罚动作：警告、禁言、封禁。
3. 填写处罚原因。
4. 点击“执行处罚”。

当前前端固定处罚时长为 7 天。

### 7.3 反馈处理

1. 在“反馈列表”查看用户反馈。
2. 点击“处理”。
3. 在弹窗中输入处理意见。
4. 提交后该反馈从当前待处理列表移除。

---

## 八、API 接口说明

### 8.1 基础规则

- 基础路径：`/api/v1`
- 请求体：JSON
- 响应体：统一包装为 `ApiResponse`
- 分页：使用 `page`、`size` 参数，前端分页页码按后端返回结果展示
- 鉴权：需要登录的接口必须携带 JWT

统一响应示例：

```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "timestamp": 1710000000000
}
```

鉴权请求头：

```http
Authorization: Bearer <token>
```

### 8.2 接口总览

| 模块 | 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|------|
| 认证 | POST | `/auth/register` | 否 | 用户注册 |
| 认证 | POST | `/auth/login` | 否 | 用户登录 |
| 认证 | POST | `/auth/sms-code` | 否 | 发送短信验证码 |
| 认证 | GET | `/auth/captcha` | 否 | 获取图形验证码 |
| 认证 | POST | `/auth/certify` | 是 | 实名认证 |
| 认证 | GET | `/auth/cert-status` | 是 | 查询认证与资料状态 |
| 认证 | PUT | `/auth/profile` | 是 | 更新个人资料 |
| 树洞 | GET | `/treehole/posts` | 否 | 动态列表 |
| 树洞 | GET | `/treehole/posts/{postId}` | 是 | 动态详情 |
| 树洞 | POST | `/treehole/posts` | 是 | 发布动态 |
| 树洞 | POST | `/treehole/posts/{postId}/like` | 是 | 点赞或取消点赞 |
| 树洞 | GET | `/treehole/posts/{postId}/comments` | 是 | 评论列表 |
| 树洞 | POST | `/treehole/posts/{postId}/comments` | 是 | 发表评论 |
| 搭子 | POST | `/partner/requests` | 是 | 发布搭子需求 |
| 搭子 | GET | `/partner/requests` | 是 | 搭子需求列表 |
| 搭子 | POST | `/partner/requests/{requestId}/apply` | 是 | 申请加入 |
| 搭子 | PUT | `/partner/requests/{requestId}/cancel` | 是 | 撤销需求 |
| 搭子 | GET | `/partner/matches` | 是 | 我的搭子匹配列表 |
| 搭子 | GET | `/partner/matches/{matchId}` | 是 | 匹配详情 |
| 搭子 | PUT | `/partner/matches/{matchId}` | 是 | 接受或拒绝申请 |
| 搭子 | POST | `/partner/matches/{matchId}/reviews` | 是 | 提交搭子评价 |
| 恋爱 | GET | `/love/requests` | 是 | 交友需求列表 |
| 恋爱 | POST | `/love/requests` | 是 | 发布交友需求 |
| 恋爱 | POST | `/love/requests/{requestId}/heart` | 是 | 发送心动 |
| 恋爱 | GET | `/love/matches` | 是 | 恋爱匹配列表 |
| 通知 | GET | `/notifications` | 是 | 通知列表 |
| 通知 | PUT | `/notifications/{id}/read` | 是 | 标记单条已读 |
| 通知 | PUT | `/notifications/read-all` | 是 | 全部已读 |
| 个人活动 | GET | `/profile/published` | 是 | 我的发布 |
| 个人活动 | GET | `/profile/applications` | 是 | 我的申请 |
| 反馈 | POST | `/feedback` | 是 | 提交反馈 |
| 反馈 | GET | `/feedback/me` | 是 | 我的反馈 |
| 管理 | GET | `/admin/reviews/pending` | 管理员 | 待审核列表 |
| 管理 | GET | `/admin/reviews/{reviewId}` | 管理员 | 审核详情 |
| 管理 | PUT | `/admin/reviews/{reviewId}` | 管理员 | 提交审核 |
| 管理 | POST | `/admin/users/{targetUserId}/ban` | 管理员 | 用户处罚 |
| 管理 | GET | `/admin/feedback` | 管理员 | 反馈列表 |
| 管理 | PUT | `/admin/feedback/{feedbackNumber}` | 管理员 | 处理反馈 |

---

## 九、数据库说明

### 9.1 开发数据库

当前开发环境使用 H2 内存数据库：

- JDBC URL：`jdbc:h2:mem:campushub;DB_CLOSE_DELAY=-1;MODE=MySQL`
- 用户名：`sa`
- 密码：空
- 控制台：`http://localhost:8080/h2-console`

连接 H2 控制台时填写：

```text
JDBC URL: jdbc:h2:mem:campushub
User Name: sa
Password: <留空>
```

### 9.2 初始化数据

后端启动时由 `DataInitializer` 自动插入：

- 4 个演示用户，其中 1 个管理员。
- 3 条已认证普通用户资料。
- 3 条树洞动态。
- 2 条搭子需求。

由于使用 H2 内存库，后端重启后数据会重新创建。

### 9.3 切换到 MySQL

修改 `backend/src/main/resources/application.yml` 中数据源配置，例如：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campushub?useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: your_password
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
```

建表 SQL 可参考 `docs/P3/04-ER图与建表SQL.md`。

---

## 十、测试与构建

### 10.1 后端测试

```bash
cd backend
mvn test
```

运行单个测试类：

```bash
mvn test -Dtest=UserServiceTest
```

运行集成测试：

```bash
mvn test -Dtest=*IntegrationTest
```

### 10.2 前端测试

```bash
cd frontend
npm test
```

### 10.3 前端代码检查

```bash
cd frontend
npm run lint
```

### 10.4 前端构建与预览

```bash
cd frontend
npm run build
npm run preview
```

---

## 十一、常见问题

### Q1：前端页面能打开，但接口请求失败

确认后端是否已启动在 `http://localhost:8080`。前端开发环境通过 Vite 代理访问 `/api/v1`，后端未启动会导致网络错误。

### Q2：前端启动提示找不到依赖

进入 `frontend` 目录执行：

```bash
npm install
```

### Q3：后端启动提示 8080 端口被占用

可关闭占用端口的进程，或修改 `backend/src/main/resources/application.yml`：

```yaml
server:
  port: 8081
```

如果修改为 8081，前端也需要设置 `VITE_API_PROXY_TARGET=http://localhost:8081`。

### Q4：登录后访问接口返回 401

可能原因：

- Token 过期，重新登录即可。
- 浏览器保留了旧 Token，可清除 `localStorage` 后重新登录。
- 后端重启后内存数据库重置，部分旧数据 ID 不再存在。

### Q5：访问管理后台被跳回首页

当前账号不是管理员。请使用 `13800138000 / Admin123` 登录。

### Q6：H2 控制台无法访问

确认：

- 后端已经启动。
- `application.yml` 中 `spring.h2.console.enabled=true`。
- 访问路径为 `http://localhost:8080/h2-console`。

### Q7：为什么重启后发布的数据消失

开发环境使用 H2 内存数据库，重启后会清空并重新初始化演示数据。需要持久化数据时请切换到 MySQL。

### Q8：发布、申请、点赞等按钮没有显示

多数写操作需要登录。请先登录普通用户账号，再访问对应页面。

---

## 十二、开发维护指南

### 12.1 后端新增功能流程

1. 在 `entity/` 新增或调整实体。
2. 在 `repository/` 新增数据访问接口。
3. 在 `service/` 编写业务逻辑。
4. 在 `controller/` 暴露 REST 接口。
5. 在 `SecurityConfig` 配置接口权限。
6. 在 `src/test/java` 增加单元测试或集成测试。

### 12.2 前端新增页面流程

1. 在 `frontend/src/views/` 新增页面组件。
2. 在 `frontend/src/router/index.js` 增加路由。
3. 在 `frontend/src/api/index.js` 或页面中调用接口。
4. 需要登录态时使用 `useAuthStore`。
5. 异步状态优先复用 `useAsyncState`，分页列表优先复用 `usePagination`。
6. 页面样式优先放入 `frontend/src/styles/` 的对应模块文件。

### 12.3 代码规范

| 类型 | 规范 |
|------|------|
| Java 包名 | `com.campushub.*` |
| Java 类名 | PascalCase |
| Java 方法名 | camelCase |
| REST 路径 | 小写路径，按模块分组 |
| JSON 字段 | lowerCamelCase |
| 数据表 | 当前 JPA 自动建表，设计文档中使用 `tb_` 前缀 |
| 前端组件 | Vue 单文件组件，页面放在 `views/` |
| 提交说明 | 按课程要求标注 `[AI-assisted]` 或 `[Human-written]` |

### 12.4 相关文档

| 文档 | 路径 |
|------|------|
| 需求规格说明书 | `docs/P1/需求规格说明书.md` |
| 架构设计文档 | `docs/P2/架构设计文档.md` |
| API 规范文档 | `docs/P3/03-API规范文档.md` |
| ER 图与建表 SQL | `docs/P3/04-ER图与建表SQL.md` |
| 详细设计文档 | `docs/P3/05-详细设计文档-整合版.md` |
| Sprint 任务看板 | `docs/P4/01-Sprint任务看板.md` |
| Bug 修复日志 | `docs/P4/04-Bug修复日志.md` |
| 演示说明 | `docs/P4/05-演示说明.md` |
