# 操作说明指南 — CampusHub（青隅）校园互助服务平台

> 版本：v1.0.0 | 更新日期：2026-05-14

---

## 目录

1. [系统概述](#一系统概述)
2. [环境准备](#二环境准备)
3. [项目结构](#三项目结构)
4. [快速启动](#四快速启动)
5. [功能操作指南](#五功能操作指南)
6. [API接口说明](#六api接口说明)
7. [数据库说明](#七数据库说明)
8. [常见问题](#八常见问题)
9. [开发指南](#九开发指南)

---

## 一、系统概述

**CampusHub（青隅）** 是一个面向高校大学生的校园互助社交平台，提供三大核心服务：

| 模块 | 功能 | 核心价值 |
|------|------|---------|
| 树洞 | 匿名心情分享、点赞评论 | 情绪宣泄、社区共鸣 |
| 找搭子 | 发布搭子需求、匹配申请、评价 | 学习/运动/出行伙伴匹配 |
| 恋爱互助 | 交友资料、心动匹配、交友需求 | 校园交友撮合 |

### 技术架构

```
┌────────────────────────┐
│   前端：Vue 3 + Vite   │  → 运行在 localhost:3000
└───────────┬────────────┘
            │ RESTful API (JSON)
┌───────────▼────────────┐
│  后端：Spring Boot 3.2 │  → 运行在 localhost:8080
│  认证：JWT + BCrypt    │
│  数据库：H2 (开发)     │
└────────────────────────┘
```

---

## 二、环境准备

### 2.1 必需软件

| 软件 | 最低版本 | 下载地址 | 验证命令 |
|------|---------|---------|---------|
| JDK | 17 | [Adoptium](https://adoptium.net/) | `java -version` |
| Maven | 3.9 | [Maven](https://maven.apache.org/) | `mvn -version` |
| Node.js | 18 | [Node.js](https://nodejs.org/) | `node -version` |
| npm | 9 | 随 Node.js 安装 | `npm -version` |

### 2.2 验证环境

```bash
# 检查Java
java -version
# 期望输出：openjdk version "17.0.x" ...

# 检查Maven
mvn -version
# 期望输出：Apache Maven 3.9.x ...

# 检查Node
node -version
# 期望输出：v18.x.x 或更高

# 检查npm
npm -version
# 期望输出：9.x.x 或更高
```

### 2.3 克隆项目

```bash
git clone <仓库地址>
cd Software-Engineering-Project
```

---

## 三、项目结构

```
Software-Engineering-Project/
├── backend/                          # 后端 Spring Boot 项目
│   ├── pom.xml                       # Maven 依赖配置
│   ├── mvnw                          # Maven Wrapper 脚本
│   └── src/
│       ├── main/
│       │   ├── java/com/campushub/
│       │   │   ├── CampusHubApplication.java    # 启动类
│       │   │   ├── common/           # 通用类（ApiResponse、异常处理）
│       │   │   ├── config/           # 配置类（Security、数据初始化）
│       │   │   ├── security/         # 安全组件（JWT工具、过滤器）
│       │   │   ├── entity/           # 实体类（20个JPA实体）
│       │   │   ├── dto/              # 数据传输对象
│       │   │   │   └── request/      # 请求DTO
│       │   │   ├── repository/       # 数据访问层（15个Repository）
│       │   │   ├── service/          # 业务逻辑层（6个Service）
│       │   │   └── controller/       # 控制器层（7个Controller）
│       │   └── resources/
│       │       └── application.yml   # 应用配置
│       └── test/                     # 测试代码
│           └── java/com/campushub/
│               ├── service/          # 单元测试
│               └── integration/      # 集成测试
├── frontend/                         # 前端 Vue 3 项目
│   ├── package.json                  # npm 依赖配置
│   ├── vite.config.js                # Vite 构建配置
│   ├── index.html                    # 入口HTML
│   └── src/
│       ├── main.js                   # Vue 应用入口
│       ├── App.vue                   # 根组件
│       ├── api/index.js              # API 请求封装
│       ├── router/index.js           # 路由配置
│       ├── store/auth.js             # 状态管理
│       ├── assets/style.css          # 全局样式
│       └── views/                    # 页面组件（13个页面）
├── docs/                             # 项目文档
│   ├── P0/                           # 项目启动文档
│   ├── P1/                           # 需求分析文档
│   ├── P2/                           # 架构设计文档
│   ├── P3/                           # 详细设计文档
│   └── P4/                           # 编码阶段文档
└── .github/workflows/                # CI/CD 配置
```

---

## 四、快速启动

### 4.1 启动后端（Spring Boot）

需下载Maven\JDK
**Windows:**
```bash
cd backend
mvn spring-boot:run
```

**Mac / Linux:**
```bash
cd backend
./mvnw spring-boot:run   # 如果已配置Maven Wrapper
# 或
mvn spring-boot:run
```

**启动成功标志：**
```
Started CampusHubApplication in X.XXX seconds
```

后端默认运行在 `http://localhost:8080`。

### 4.2 启动前端（Vue 3）

```bash
cd frontend
npm install        # 首次运行需安装依赖（仅需一次）
npm run dev
```

**启动成功标志：**
```
VITE v5.x.x  ready in XXX ms
➜  Local:   http://localhost:3000/
```

### 4.3 访问系统

打开浏览器访问：**http://localhost:3000**

### 4.4 登录演示

在登录页面输入演示账号：

| 角色 | 手机号 | 密码 |
|------|--------|------|
| 普通用户1（小明） | 13800138001 | Abc12345 |
| 普通用户2（小红） | 13800138002 | Abc12345 |
| 管理员 | 13800138000 | Admin123 |

---

## 五、功能操作指南

### 5.1 注册新账号

1. 访问 `http://localhost:3000/#/register`
2. 填写手机号（11位）、用户名（4-16位中英文数字）、密码（≥8位，含字母和数字）
3. 短信验证码输入 `123456`（演示环境固定验证码）
4. 勾选同意用户协议，点击"注册"
5. 注册成功后自动跳转登录页

### 5.2 实名认证

> 发布搭子需求、浏览交友资料等功能需要先完成实名认证。

1. 登录后进入"个人中心"
2. 在"实名认证"区域填写：
   - 学号、真实姓名、身份证号
   - 学校、专业、年级
   - 性别、年龄
3. 点击"提交认证"

### 5.3 树洞功能

#### 浏览树洞
1. 点击导航栏"树洞"
2. 可按分类筛选：情感/学习/生活/趣味/其他
3. 可搜索关键词
4. 可按"最新发布"或"最热"排序

#### 发布动态
1. 在树洞页面点击"发布动态"
2. 选择分类，输入内容（最长500字）
3. 可勾选"匿名发布"（系统自动生成匿名昵称如"匿名小友327"）
4. 点击"发布"

#### 点赞和评论
1. 点击动态进入详情页
2. 点击"点赞"按钮
3. 在评论区域输入内容（最长100字），点击"发表评论"

### 5.4 找搭子功能

#### 浏览搭子需求
1. 点击导航栏"找搭子"
2. 可按类型筛选：学习/运动/吃饭/考试/出行/其他
3. 可搜索关键词

#### 发布搭子需求
1. 在找搭子页面点击"发布需求"
2. 填写：
   - 搭子类型（选择）
   - 需求描述（10-200字）
   - 匹配条件（JSON格式，可选）
   - 有效天数（1-7天）
   - 最大人数（1-10人）
3. 点击"发布"

#### 申请匹配（接单）
1. 浏览搭子需求列表
2. 对感兴趣的需求点击"申请匹配"
3. 输入申请附言（可选），点击确定
4. 等待需求发布者响应

#### 查看匹配详情
需要先有匹配记录，可通过API直接访问匹配ID。

#### 提交评价
匹配建立后，可对搭子进行1-5星评价，评价内容10-100字。

### 5.5 恋爱互助功能

#### 完善交友资料
1. 进入"个人中心"（需先完成实名认证）
2. 在"完善交友资料"区域填写：
   - 性别、年龄、身高
   - 择偶标准（JSON格式）
   - 交友宣言（10-100字）
3. 点击"保存"

#### 浏览交友资料
1. 点击导航栏"恋爱互助"
2. 可按性别筛选
3. 查看他人资料和交友宣言

#### 发布交友需求
1. 在恋爱互助页面点击"发布交友需求"
2. 填写需求描述和有效天数
3. 点击"发布"

#### 发送心动
对感兴趣的交友需求发送心动（单向，对方不感知）。

#### 查看匹配
点击"我的匹配"查看双向心动匹配列表。

### 5.6 消息通知

1. 点击导航栏"通知"
2. 未读通知会高亮显示
3. 点击"标记已读"可将单条通知标记为已读
4. 点击"全部已读"可一次性标记所有通知

### 5.7 提交反馈

1. 进入"个人中心"
2. 在"提交反馈"区域选择类型：
   - 问题反馈（bug）
   - 功能建议（suggestion）
   - 内容举报（content）
   - 其他（other）
3. 填写详细描述（10-500字）
4. 点击"提交反馈"

### 5.8 管理员功能

> 需要使用管理员账号登录（13800138000 / Admin123）

#### 内容审核
1. 点击导航栏"管理"
2. 在"待审核内容"区域查看待审核列表
3. 点击"通过"或"驳回"处理审核

#### 用户管理
1. 输入要处理的用户ID
2. 选择处罚类型：警告 / 禁言 / 封禁
3. 填写处罚原因
4. 点击"执行处罚"

#### 反馈处理
1. 在"反馈列表"查看所有用户反馈
2. 点击"处理"，输入处理意见
3. 系统自动更新反馈状态

---

## 六、API接口说明

### 6.1 接口总览

全部接口遵循 RESTful 规范，基础路径：`/api/v1`

| 模块 | 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|------|
| 认证 | POST | /auth/register | 否 | 用户注册 |
| 认证 | POST | /auth/login | 否 | 用户登录 |
| 认证 | POST | /auth/sms-code | 否 | 发送短信验证码 |
| 认证 | GET | /auth/captcha | 否 | 获取图形验证码 |
| 认证 | POST | /auth/certify | 是 | 实名认证 |
| 认证 | GET | /auth/cert-status | 是 | 查询认证状态 |
| 树洞 | GET | /treehole/posts | 否 | 动态列表 |
| 树洞 | GET | /treehole/posts/{id} | 否 | 动态详情 |
| 树洞 | POST | /treehole/posts | 是 | 发布动态 |
| 树洞 | POST | /treehole/posts/{id}/like | 是 | 点赞/取消 |
| 树洞 | GET | /treehole/posts/{id}/comments | 否 | 评论列表 |
| 树洞 | POST | /treehole/posts/{id}/comments | 是 | 发表评论 |
| 搭子 | POST | /partner/requests | 是 | 发布搭子需求 |
| 搭子 | GET | /partner/requests | 是 | 需求列表 |
| 搭子 | POST | /partner/requests/{id}/apply | 是 | 申请匹配 |
| 搭子 | GET | /partner/matches/{id} | 是 | 匹配详情 |
| 搭子 | POST | /partner/matches/{id}/reviews | 是 | 提交评价 |
| 恋爱 | PUT | /love/profiles/me | 是 | 更新交友资料 |
| 恋爱 | GET | /love/profiles | 是 | 浏览交友资料 |
| 恋爱 | POST | /love/requests | 是 | 发布交友需求 |
| 恋爱 | POST | /love/requests/{id}/heart | 是 | 发送心动 |
| 恋爱 | GET | /love/matches | 是 | 匹配列表 |
| 通知 | GET | /notifications | 是 | 通知列表 |
| 通知 | PUT | /notifications/{id}/read | 是 | 标记已读 |
| 通知 | PUT | /notifications/read-all | 是 | 全部已读 |
| 反馈 | POST | /feedback | 是 | 提交反馈 |
| 反馈 | GET | /feedback/me | 是 | 我的反馈 |
| 管理 | GET | /admin/reviews/pending | 是(管理员) | 待审核列表 |
| 管理 | GET | /admin/reviews/{id} | 是(管理员) | 审核详情 |
| 管理 | PUT | /admin/reviews/{id} | 是(管理员) | 提交审核 |
| 管理 | POST | /admin/users/{id}/ban | 是(管理员) | 用户处罚 |
| 管理 | GET | /admin/feedback | 是(管理员) | 反馈列表 |
| 管理 | PUT | /admin/feedback/{id} | 是(管理员) | 处理反馈 |

### 6.2 统一响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": { ... },
  "timestamp": 1700000000000
}
```

### 6.3 认证方式

在请求头中携带 JWT Token：

```
Authorization: Bearer <token>
```

### 6.4 分页参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| page | integer | 1 | 页码（从1开始） |
| size | integer | 20 | 每页条数（最大50） |

---

## 七、数据库说明

### 7.1 开发环境

当前开发环境使用 **H2 内存数据库**，数据保存在内存中，应用重启后数据会清空。

H2 控制台地址：`http://localhost:8080/h2-console`

连接参数：
- JDBC URL：`jdbc:h2:mem:campushub`
- 用户名：`sa`
- 密码：（空）

### 7.2 初始化数据

应用启动时自动插入以下测试数据：
- 4个用户（3个普通用户 + 1个管理员）
- 3条树洞动态
- 2条搭子需求
- 3条实名认证记录

### 7.3 切换到 MySQL（生产环境）

修改 `backend/src/main/resources/application.yml`：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campushub?useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: your_password
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
```

建表SQL参见 `docs/P3/04-ER图与建表SQL.md`。

---

## 八、常见问题

### Q1：前端启动报错 "Cannot find module 'vue'"

**解决：** 运行 `npm install` 安装依赖后再启动。

### Q2：后端启动报错 "Address already in use"

**解决：** 端口8080被占用，可在 `application.yml` 中修改 `server.port`。

### Q3：登录后接口返回401

**可能原因：**
1. Token 已过期（有效期2小时），需重新登录
2. 浏览器缓存了旧的无效 Token，清除 localStorage 后重试

### Q4：前端请求接口返回 CORS 错误

**解决：** 确认后端 `SecurityConfig` 中 CORS 配置已开启，或使用 Vite 代理（开发环境默认已配置）。

### Q5：H2 控制台无法访问

**解决：** 确认 `application.yml` 中 `spring.h2.console.enabled=true`，且在 `SecurityConfig` 中已允许 `/h2-console/**` 路径。

### Q6：发布搭子需求提示"请先完成实名认证"

**解决：** 先进入"个人中心"填写实名认证信息并提交。

### Q7：管理员接口403

**解决：** 确认使用管理员账号登录（13800138000 / Admin123）。Admin接口需要管理员权限。

### Q8：如何重置数据库？

**解决：** 重启后端应用即可。H2内存数据库会在每次重启时重新创建并插入测试数据。

---

## 九、开发指南

### 9.1 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 后端框架 | Spring Boot | 3.2.5 |
| 安全框架 | Spring Security + JWT | — |
| ORM | Spring Data JPA (Hibernate) | — |
| 密码加密 | BCrypt | — |
| JWT库 | jjwt | 0.12.5 |
| 数据库 | H2 (开发) / MySQL 8.0 (生产) | — |
| 前端框架 | Vue 3 (Composition API) | 3.4 |
| 构建工具 | Vite | 5.2 |
| 状态管理 | Pinia | 2.1 |
| 路由 | Vue Router | 4.3 |
| HTTP客户端 | Axios | 1.7 |

### 9.2 添加新功能的步骤

**后端：**
1. 在 `entity/` 创建实体类（如果涉及新表）
2. 在 `repository/` 创建 Repository 接口
3. 在 `service/` 中添加业务逻辑
4. 在 `controller/` 中添加API端点
5. 在 `SecurityConfig` 中配置权限（如需要）

**前端：**
1. 在 `views/` 创建新页面组件
2. 在 `router/index.js` 中添加路由
3. 在 `api/index.js`（或新建api模块）中添加接口调用
4. 在导航栏添加入口链接（App.vue）

### 9.3 运行测试

```bash
# 运行所有测试
cd backend
mvn test

# 运行特定测试类
mvn test -Dtest=UserServiceTest

# 运行集成测试
mvn test -Dtest=*IntegrationTest
```

### 9.4 代码规范

| 规范 | 说明 |
|------|------|
| 包命名 | `com.campushub.{module}` |
| 类命名 | PascalCase（如 `UserService`） |
| 方法命名 | camelCase（如 `findByPhone`） |
| URL路径 | 短横线分隔（如 `/partner-requests`） |
| JSON字段 | lowerCamelCase（如 `createdAt`） |
| 数据库表名 | 蛇形命名 `tb_` 前缀（如 `tb_user`） |
| 提交信息 | 必须标注 `[AI-assisted]` 或 `[Human-written]` |

### 9.5 调试技巧

1. **后端调试：** 使用 IDE（IntelliJ IDEA / VS Code）设置断点，或在 `application.yml` 中设置 `spring.jpa.show-sql=true` 查看SQL
2. **前端调试：** 使用浏览器开发者工具（F12），Network 面板查看API请求响应
3. **H2控制台：** 访问 `http://localhost:8080/h2-console` 直接查看数据库状态
4. **JWT调试：** 在 [jwt.io](https://jwt.io/) 上粘贴Token查看其内容

---

## 附录：联系方式

如遇到本指南未覆盖的问题，请联系开发团队或查看项目文档目录 `docs/` 下的详细设计文档。

| 文档 | 路径 |
|------|------|
| 需求规格说明书 | `docs/P1/需求规格说明书.md` |
| 架构设计文档 | `docs/P2/架构设计文档.md` |
| API规范文档 | `docs/P3/03-API规范文档.md` |
| ER图与建表SQL | `docs/P3/04-ER图与建表SQL.md` |
| Sprint任务看板 | `docs/P4/01-Sprint任务看板.md` |
| AI代码信任度实验 | `docs/P4/02-AI代码信任度实验报告.md` |
| AI调试对决实验 | `docs/P4/03-AI调试对决实验报告.md` |
| Bug修复日志 | `docs/P4/04-Bug修复日志.md` |
